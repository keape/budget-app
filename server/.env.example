# MongoDB Atlas Connection String
# Replace <username> and <password> with your actual credentials from MongoDB Atlas
# Make sure to URL-encode special characters in password
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/budget-app?retryWrites=true&w=majority&appName=budgetapp

# JWT Secret Key for authentication tokens
# Use a strong, random secret key for production
JWT_SECRET=your-super-secret-jwt-key-here

# Server port
PORT=5001
